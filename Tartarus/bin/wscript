def build(bld):
    bins = ('t-dhcp',)
    sbins = (
            'Tartarus-srv1',
            'Tartarus-join',
            'Tartarus-leave',
            't-deploy-srv')
    bld.install_files('${BINDIR}', bins, chmod=0755)
    bld.install_files('${SBINDIR}', sbins, chmod=0755)

